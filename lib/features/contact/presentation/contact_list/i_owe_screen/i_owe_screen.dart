import 'package:flutter/material.dart';

class IOweScreen extends StatelessWidget {
  const IOweScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('I owe'),
    );
  }
}