import 'package:flutter/material.dart';

class OwedToMeScreen extends StatelessWidget {
  const OwedToMeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Owed To Me'),
    );
  }
}